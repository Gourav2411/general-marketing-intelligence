import { Client } from "@modelcontextprotocol/sdk/client/index.js";
import { StdioClientTransport } from "@modelcontextprotocol/sdk/client/stdio.js";
import { existsSync, readFileSync } from "node:fs";
import { join } from "node:path";
const snippet=join(process.cwd(),"setup-output/claude-desktop-snippet.json"),configured=existsSync(snippet)?JSON.parse(readFileSync(snippet,"utf8")).mcpServers["general-marketing-intelligence"]:{command:process.execPath,args:[join(process.cwd(),"dist/index.js")],env:{...process.env,DATA_MODE:process.env.DATA_MODE??"demo",AI_PROVIDER:"none"}},transport=new StdioClientTransport({...configured,env:{...process.env,...configured.env},cwd:process.cwd(),stderr:"inherit"}),client=new Client({name:"gmi-doctor",version:"1.0.0"});await client.connect(transport);const result=await client.callTool({name:"diagnose_setup",arguments:{test_google_access:process.argv.includes("--live")}});console.log(result.content?.[0]?.text??"No diagnostic output");await transport.close();
