import assert from "node:assert/strict";
import { Client } from "@modelcontextprotocol/sdk/client/index.js";
import { StdioClientTransport } from "@modelcontextprotocol/sdk/client/stdio.js";
const transport=new StdioClientTransport({command:process.execPath,args:["dist/stdio.js"],cwd:process.cwd(),stderr:"inherit"}),client=new Client({name:"gmi-schema-compat",version:"1.0.0"});await client.connect(transport);const {tools}=await client.listTools(),byName=new Map(tools.map(tool=>[tool.name,tool]));
const required={marketing_intelligence_router:["question"],ad_strategy_review:["campaigns","max_daily_change_pct"],paid_media_diagnostics:["rows"],paid_media_csv_report:["path","max_daily_change_pct"],meta_ads_report:["start_date","end_date","row_limit"],linkedin_ads_report:["start_date","end_date","row_limit"],generic_crm_api_report:["config_path","start_date","end_date","row_limit"],google_ads_strategy_review:["campaign_type","objective","target_cpa","target_roas","conversion_lag_days","max_daily_change_pct"],google_search_console_report:["comparison","country","device","search_type","page_filter","query_filter","row_limit"],ga4_acquisition_report:["comparison","country","device","landing_page_filter","campaign_filter","channel_group","row_limit"],executive_growth_review:["days","row_limit"],experiment_review:["name","success_threshold"],keyword_opportunity_research:["days","brand_terms","limit"],derive_growth_strategy:["objective","horizon_days"],derive_marketing_strategy:["audience","positioning_hypothesis","horizon_days"],build_paid_campaign_plan:["name","objective","audience","offer","landing_page","budget","keywords","match_types"],build_email_campaign_plan:["name","objective","audience","offer","audience_size","cta"],build_dashboard:["title","charts"],build_dashboard_chart:["dataset","dimension","metric","aggregation","chart","filter","limit","sort","start_date","end_date","comparison","title"],preview_marketing_action:["kind","source","summary","payload","risk"],approve_marketing_action:["approval_id","confirmation"],execute_approved_action:["approval_id"],revoke_action_approval:["approval_id"],action_audit_history:["limit"]};
for(const [name,properties] of Object.entries(required)){const schema=byName.get(name)?.inputSchema;assert.ok(schema,`Missing stable tool ${name}`);for(const property of properties)assert.ok(schema.properties?.[property],`${name} lost input ${property}`)}assert.equal(tools.length,54,"Unexpected public tool count; update the compatibility contract intentionally for a versioned schema change");await transport.close();console.log("MCP public schema compatibility checks passed.");
